import { testTest } from "../src/client/js/graphLoader"

describe("Test to ensure that the function exists", () => {
    test("Should return true", () => {
        expect(testTest).toBeDefined();
    });
});